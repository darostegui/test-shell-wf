name: TEST
on:
  push:
jobs:
  TEST:
    runs-on: self-hosted
    steps:
      - name: Test Command
        shell: tcsh {0}
        run: |
          echo "GitHub Runner running test stage"
          echo "echo 'Hello World'"> /tmp/sourceme
          cat /tmp/sourceme
          source /tmp/sourceme

